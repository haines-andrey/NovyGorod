@using Microsoft.AspNetCore.Mvc.TagHelpers
@using NovyGorod.Domain.Models.Posts
@using Microsoft.AspNetCore.Mvc.Localization
@using NovyGorod.Application.Contracts.Posts.Requests
@inject IViewLocalizer Localizer
@{
    const int pageIndex = 0;
    const int pageSize = 6;
}

<nav class="navbar navbar-expand-md navbar-light py-1">
    <div class="container-fluid p-0">
        <a class="navbar-brand p-0 m-0" asp-controller="Home" asp-action="Index">
            <img class="align-top img-fluid" style="max-height:60px" src="~/images/logo.png" alt="logo"/>
        </a>
        <button class="navbar-toggler"
                type="button"
                data-bs-toggle="collapse"
                data-bs-target="#navbarSupportedContent"
                aria-controls="navbarSupportedContent"
                aria-expanded="false"
                aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav ms-auto">
                <li class="nav-item">
                    <a class="nav-link text-dark"
                       href="@Url.Action("Index", "Home")">
                        @Localizer["About us"]
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link text-dark"
                       href="@Url.Action("ViewList", "Post", new SearchPostsRequest {Type = PostType.Project, PageIndex = pageIndex, PageSize = pageSize})">
                        @Localizer["Projects"]
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link text-dark"
                       href="@Url.Action("ViewList", "Post", new SearchPostsRequest {Type = PostType.Theatre, PageIndex = pageIndex, PageSize = pageSize})">
                        @Localizer["Theatre"]
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link text-dark"
                       href="@Url.Action("ViewList", "Post", new SearchPostsRequest {Type = PostType.School, PageIndex = pageIndex, PageSize = pageSize})">
                        @Localizer["School"]
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link text-dark"
                       href="@Url.Action("ViewList", "Post", new SearchPostsRequest {Type = PostType.Festival, PageIndex = pageIndex, PageSize = pageSize})">
                        @Localizer["Festivals"]
                    </a>
                </li>
            </ul>
            @* <div class="dropdown ml-auto"> *@
            @*     <button class="btn btn-sm btn-outline-secondary dropdown-toggle" *@
            @*             type="button" *@
            @*             id="dropdownMenuButton" *@
            @*             data-toggle="dropdown" *@
            @*             aria-haspopup="true" *@
            @*             aria-expanded="false"> *@
            @*         Translate *@
            @*     </button> *@
            @*     <div class="dropdown-menu" aria-labelledby="dropdownMenuButton"> *@
            @*         <a class="dropdown-item" *@
            @*            asp-all-route-data="@Context.GetRouteData().Values.ToDictionary(x => x.Key, x => x.Value.ToString())" *@
            @*            asp-route-culture="en"> *@
            @*             English *@
            @*         </a> *@
            @*         <a class="dropdown-item" *@
            @*            asp-all-route-data="@Context.GetRouteData().Values.ToDictionary(x => x.Key, x => x.Value.ToString())" *@
            @*            asp-route-culture="ru"> *@
            @*             Русский *@
            @*         </a> *@
            @*     </div> *@
            @* </div> *@
        </div>
    </div>
</nav>